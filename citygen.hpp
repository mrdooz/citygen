#pragma once

#include <vector>
#include <map>
#include <string>
#include <stdint.h>

#include <glm/glm.hpp>
#include <glm/core/type_vec3.hpp>

typedef uint8_t u8;

namespace citygen
{
  using std::vector;
  using std::map;
  using std::string;

  using glm::vec3;
  using glm::vec4;
  using glm::mat4;
}